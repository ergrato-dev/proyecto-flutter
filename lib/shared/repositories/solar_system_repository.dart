/// Repositorio que accede a la API Solar System OpenData.
///
/// @what Provee datos orbitales y físicos de planetas, lunas, asteroides y cometas.
/// @why No requiere autenticación; centraliza la lógica de serialización de
///   los cuerpos del sistema solar para el módulo `lists/`.
/// @impact Usado por el módulo `lists/` y la pantalla de detalle de cuerpos.
library;

// TODO(fase-2): implementar con Dio + https://api.le-systeme-solaire.net/rest.
class SolarSystemRepository {
  // Stub — se implementa en la Fase 2 (módulo lists/).
}
