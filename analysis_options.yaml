# Configuración del analizador estático de Dart para CosmosFlutter.
# Base: flutter_lints (recomendado por el equipo de Flutter).
# Reglas adicionales: refuerzan null safety, documentación y buenas prácticas.
include: package:flutter_lints/flutter.yaml

analyzer:
  # Tratar advertencias del analizador como errores (fail-fast en CI).
  errors:
    missing_required_param: error
    missing_return: error
    todo: ignore
  exclude:
    # Excluir código generado por flutter pub run build_runner.
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    # ── Calidad de código ──────────────────────────────────────────────────────
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_empty_else: true
    avoid_print: true
    avoid_redundant_argument_values: true
    avoid_returning_null_for_void: true
    avoid_type_to_string: true
    avoid_unused_constructor_parameters: true
    cancel_subscriptions: true
    close_sinks: true
    directives_ordering: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_single_quotes: true
    sized_box_for_whitespace: true
    sort_child_properties_last: true
    unawaited_futures: true
    unnecessary_await_in_return: true
    unnecessary_lambdas: true
    use_if_null_to_convert_nulls_to_bools: true
    # ── Null safety ────────────────────────────────────────────────────────────
    avoid_null_checks_in_equality_operators: true
